
server {
    listen <%= nginx_port %>;
    server_name <%= define_servername %><% nginx_serveraliases.each do |val| -%> <%= val %><% end -%>;
    rewrite ^(.*)$ <%= nginx_redirecturl %>$1 <%= define_permanent %>;
}

